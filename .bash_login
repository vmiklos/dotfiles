#!/bin/bash

# avoid idiotic dos-like prompt
PS1='\u@\h:\w\$ '
# ls colors via ssh
export LS_OPTIONS=$(echo $LS_OPTIONS|sed 's/--color=[^ ]\+/--color=auto/')
# silence cpm warnings
alias cpm="sudo ~/bin/cpm"
