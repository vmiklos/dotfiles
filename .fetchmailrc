set daemon 30

poll gwmail.emea.novell.com proto imap
        user 'vmiklos' password 'SUSECOMPASSWORD'
        options ssl fetchall nokeep
        mda "/usr/bin/procmail /home/vmiklos/.procmailrc.susecom"

poll gwmail.emea.novell.com proto imap
        user 'vmiklos' password 'SUSECOMPASSWORD'
        options ssl folder Calendar keep
        mda "/usr/bin/procmail /home/vmiklos/.procmailrc.susecom-calendar"

poll imap.npsh.hu proto imap
        user 'mvajna' password 'SUSEHUPASSWORD'
        options ssl fetchall nokeep
        mda "/usr/bin/procmail /home/vmiklos/.procmailrc.susehu"

poll imap.npsh.hu proto imap
        user 'mvajna' password 'SUSEHUPASSWORD'
        options ssl folder Calendar keep
        mda "/usr/bin/procmail /home/vmiklos/.procmailrc.susehu-calendar"
